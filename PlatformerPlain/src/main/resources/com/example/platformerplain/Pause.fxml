<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<GridPane
        fx:id="root"
        alignment="CENTER"
        prefHeight="720.0"
        prefWidth="1280.0"
        stylesheets="@styles.css"
        xmlns:fx="http://javafx.com/fxml/1"
        xmlns="http://javafx.com/javafx/22"
        fx:controller="com.example.platformerplain.controller.PauseScreenController">

    <!-- Define one column and vertically distribute rows equally -->

    <!-- Centralizing all buttons using VBox -->
    <VBox alignment="CENTER" spacing="10" GridPane.rowIndex="2">
        <Button
                fx:id="ResumeButton"
                onAction="#handleResume"
                text="Resume"
                prefWidth="200.0"
        />

        <Button
                fx:id="MenuButton"
                onAction="#handleMenu"
                text="Menu"
                mnemonicParsing="false"
                prefWidth="200.0"
        />

        <Button
                fx:id="ExitButton"
                onAction="#handleExitGame"
                text="Exit"
                prefWidth="200.0"
        />

        <Button
                fx:id="RestartButton"
                onAction="#handleRestart"
                text="Restart"
                mnemonicParsing="false"
                prefWidth="200.0"
        />

        <Button
                fx:id="helpButton"
                onAction="#handleHelp"
                text="Help"
                prefWidth="200.0"
        />
    </VBox>

</GridPane>
